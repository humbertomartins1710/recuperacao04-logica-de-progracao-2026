import leia from 'readline-sync'
var objeto = [{
    livro:null,
    ano:null,
}]
var cadastro = leia.questionInt("quantos fimes serao cadastrados")

while(cadastro != 0){
    var nome =leia.question("qual o nome do livro")
    var ano = leia.questionInt("em que ano foi lancado")

    

}